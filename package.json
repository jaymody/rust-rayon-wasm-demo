{
  "private": true,
  "scripts": {
    "serve": "wasm-pack build --target web --release && wasm-pack build --target web --release --out-dir pkg-parallel -- --features parallel && webpack-dev-server --open"
  },
  "devDependencies": {
    "html-webpack-plugin": "^5.5.1",
    "wasm-pack": "^0.11.1",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^4.15.0"
  },
  "dependencies": {
    "comlink": "^4.3.1",
    "wasm-feature-detect": "^1.3.0"
  }
}
